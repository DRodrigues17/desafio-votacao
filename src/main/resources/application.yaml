server:
  servlet:
    context-path: /v1
spring:
  datasource:
    username: usuario
    url: jdbc:postgresql://localhost:5432/db-desafio-votacao
    driverClassName: org.postgresql.Driver
    password: senha

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
    show-sql: 'false'